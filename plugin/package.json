{
  "name": "web-to-obsidian-plugin",
  "version": "0.7.0",
  "private": true,
  "description": "Hermes plugin package: declares the web-clip-extractor npm dependency so a plugin install pulls a matching Node.js extractor version into plugin/node_modules.",
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@tiny-codes/web-clip-extractor": "^0.2.0"
  }
}